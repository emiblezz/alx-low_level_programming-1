#include <stdio.h>

/**
 *
 *
 */

int main(void)
{
	int ones = '0';
	int tens = 'o';

	for (tens = '0'; tens <= '9'; tens++)
	{
		for (ones = '0'; ones <= '9'; ones++)
		{
			putchar(tens);
			putchar(ones);
			if ((tens != '9') && (ones != '9'))
			{
				putchar(',');
				putchar(' ');
			}
		}
	}
	putchar('\n');

	return (0);
}
