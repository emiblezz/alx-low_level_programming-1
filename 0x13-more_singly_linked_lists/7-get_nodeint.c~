#include "lists.h"

listint_t *get_nodeint_at_index(listint_t *head, unsigned int index)
{
	unsigned int count = 0;
	listint_t temp;

	if (head == NULL)
		return (NULL);

	while (index <= count)
	{
		temp = *head;
		head = temp->next;
		count++;
	}

	return (head);
}
